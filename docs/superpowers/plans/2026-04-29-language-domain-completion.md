# Language Domain Completion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring the currently documented language/domain surface closer to `docs/SPEC.md` before adding more e2e fixtures.

**Architecture:** Keep the existing pipeline order: lexer/parser accepts source surface, resolver/analyzer preserves or validates it conservatively, runtime implements only core executable domains. Advanced domains not implemented in this phase must produce explicit diagnostics or stable no-op behavior instead of parser failures.

**Tech Stack:** Rust workspace (`orv-syntax`, `orv-resolve`, `orv-analyzer`, `orv-runtime`), `rtk cargo test`, `orv-cli check` fixtures.

**Status (2026-04-30):** This implementation pass is complete and superseded by the current codebase state. Parser surface gaps, core server/web/db runtime paths, advanced-domain reference stubs, fixture checks, HIR origin map generation with `contains`/`calls` edges, AST ProjectGraph v1 plus source/semantic graph depth stats, `orv test` reference test block runner bootstrap, `orv lsp snapshot` editor bootstrap JSON, `orv lsp reveal` production-to-LSP-location JSON, `orv lsp serve --stdio` Content-Length JSON-RPC initialize/shutdown/documentSymbol/diagnostic/definition/references/hover/completion bootstrap with didOpen/didChange full-sync open-buffer cache, `orv dap serve --stdio` Debug Adapter Protocol initialize/launch/configurationDone/setBreakpoints/setFunctionBreakpoints/dataBreakpointInfo/setDataBreakpoints/threads/stackTrace/scopes/variables/loadedSources/continue/step/disconnect/terminate bootstrap with project-graph-backed entry stack frame, function/data breakpoint frames, project variables, and reference runtime stdout/status/error variables, `orv.toml` `[project].entry` source-entry resolution with project-directory input, `orv init <dir>` minimal scaffold generation, initial `orv build` manifest/bundle-plan/origin-map/project-graph/source-bundle/source-bundled server-runtime/launch artifacts with `runtime_features`, `orv build --prod` deploy manifest/reference server entrypoint, artifact verification, import-aware artifact source reanalysis, source-bundled artifact reference execution, build-directory launcher/static execution, HTML-only zero-runtime static page output, client manifest/reactive-plan/page/JS/WASM bundle targets with source-bound `client/app.wasm` initial-render memory exports, build directory verification, `orv reveal <dir> <origin-id>` build artifact route/client bundle origin reveal with source-bundle fallback, explicit `@db.save/load` JSON snapshots, `@db.wal(path)` JSONL append+fsync replay, `@db.checkpoint()` WAL snapshot compaction, explicit `@db.savepoint()`/`@db.rollback(point)` memory-state restoration, WAL-backed savepoint and transaction rollback replay preservation, torn final WAL record recovery, `orv db plan/verify` schema migration dry-run/drift check, `orv db apply/migrate` schema snapshot updates with optional migration history, `orv db squash` history action compaction, `orv db migrate --data` JSON snapshot add/drop field execution, `orv db rollback` schema/data snapshot restoration, `orv db backup/restore` local JSON data snapshot artifacts, and runtime `@ffi`/`@net` method enforcement inside `@unsafe` were implemented in the working tree. Remaining gaps are now higher-level compiler/platform work: native production codegen/bundling, real client WASM/JS codegen/glue, full LSP/DAP method set, editor UI production reveal, HTTP/2/H3/QUIC transport, persistent DB external adapters/WAL archive/PITR/full crash matrix, ABI signature/native FFI loading, and real plugin sandboxing.

**DAP delta (2026-05-02):** DAP bootstrap now verifies breakpoints against ProjectGraph selectable nodes plus nested AST statement lines, evaluates conditional and hit-count breakpoints against Locals values, accepts function breakpoints against captured runtime call-stack names, accepts data breakpoints against Locals value changes, accepts ORV diagnostics/runtime exception filters, captures reference-runtime debug frames after executed HIR statements with active function call stack and per-frame stdout deltas, exposes runtime-evaluated Locals through `scopes`/`variables`, makes current frame locals available to `evaluate` and `completions`, exposes direct function call choices through `stepInTargets`, advances `next`/`stepIn`/`stepOut` through captured runtime frames with optional captured `stepIn.targetId`, rewinds the current captured function frame with `restartFrame`, continues to the next verified line/function/data breakpoint when one remains, supports `restart`, emits stdout/stderr `output` events, and emits stdio `initialized`/`stopped`/`continued`/`terminated` event frames. 2026-05-03 adds opt-in `launch.arguments.live=true` DebugStepper progression so launch does not pre-run the full program, `next`/`stepIn`/`stepOut`/`continue` incrementally advance runtime frames, live `stepIn.targetId` is rejected instead of ignored, `restart` preserves live mode unless explicitly overridden, and `@server` launch opens a non-blocking paused long-running frame with `continue`/`pause` events instead of starting the HTTP accept loop by default. It also exposes long-running async runtime kind/state, static/env listen endpoint, route inventory, transport process state/id/address, and pause/resume counters through launch runtime JSON, `variables`, `evaluate`, and `completions`, exposes in-process request frame count/last/list/trace and request trace file path through `variables`, `evaluate`, and `completions`, serves DAP `source` content from the launch-time project source snapshot, rejects debug-control requests for unknown thread ids, advertises `supportsOrvRuntimeAttach` and `supportsOrvRuntimeTracePath`, and supports opt-in `launch.arguments.attachRuntime=true` child-process accept-loop attach plus `attachRuntimeMode="inProcess"` attached server thread transport. Remaining DAP work is richer native editor UI rendering, not protocol coverage.

**Dev/HMR delta (2026-05-03):** `orv dev --hmr` now emits `dev/session.json` with source hash watch inputs, bundle targets, hot-reload/full-reload fallback strategy, `dev/transport.json` reference browser/server transport contract, and `dev/hmr-client.js` EventSource reload client. `orv dev --watch` emits `dev/watch.json` with poll-loop/watch target contract, and `orv dev --watch-loop` runs a poll loop that records `dev/events.json` rebuild/skip events with a manifest transport. `orv dev --hmr --serve` now starts the reference HTTP/1 HMR endpoint, writes `dev/server.json`, serves `/__orv/hmr/session` JSON, and streams `/__orv/hmr/events` as EventSource-compatible events. `orv verify-build` validates these dev manifests when present.

**DB archive delta (2026-05-03):** `orv db recover` now accepts `--archive <archive.json>` as an alternative to raw `--wal`, resolves file archive targets, verifies archived WAL hash/byte count before replay, and resolves relative source WAL paths against the archive manifest directory. `orv db restore --wal <db.wal.jsonl> --data <data.json> --at <RFC3339>` and `orv db restore --archive <archive.json> --data <data.json> --at <RFC3339>` expose raw/hash-verified archive replay as PITR restore surfaces. Remaining DB persistence work is remote archive targets, external adapters, and full crash matrix coverage.

**DB adapter safety delta (2026-05-03):** `@db.connect` now accepts the reference `memory://` adapter and rejects external adapter URLs such as `postgres://` until real adapter implementations exist, avoiding a false-success in-memory handle for production-looking connection strings.

**DB file adapter delta (2026-05-05):** `@db.connect "file://path/to/db.wal.jsonl"` now opens a local WAL-backed handle using the existing WAL replay/append/fsync path, so adapter-style DB handles can persist across runtime runs. Prod deploy persistence scans these file-adapter WAL paths as well as direct `@db.wal` calls and emits matching Compose mounts for relative parent directories. PostgreSQL/MySQL adapters remain explicit unsupported errors.

**DB SQLite adapter delta (2026-05-05/2026-05-06):** `@db.connect "sqlite://path/to/app.sqlite"` now opens a real SQLite file with ORV metadata/row JSON tables, reloads persisted rows on later runtime runs, and syncs create/update/delete snapshots after mutation while preserving the current reference query semantics. Prod deploy persistence scans relative SQLite adapter paths into `db_paths`, mounts their parent directories, and documents them in the deploy runbook. Env-configured DB handles such as `@db.connect(@env.SHOP_DATABASE_URL ?? "sqlite://data/shop.sqlite")` record the env/default pair as `db_env`, emit Compose environment defaults, and still mount the fallback SQLite path.

**Runtime feature manifest delta (2026-05-05):** Build, server runtime, and deploy artifacts now record `db_adapter` for `@db.connect`, `payment_adapter` for `@payment.connect`, and `shipping_adapter` for `@shipping.connect`, so deploy/bundling surfaces can distinguish adapter-backed projects from pure in-memory/runtime-only features.

**Commerce adapter delta (2026-05-05/2026-05-06):** `@payment.connect("file://path/to/payments.jsonl").capture(...)` and `@shipping.connect("file://path/to/shipments.jsonl").book(...)` append reference capture/booking records to local JSONL files and fsync them. Prod deploy persistence scans these relative file adapter paths into `record_paths` and emits matching Compose mounts. 2026-05-06 adds reference HTTP adapter handles: `@payment.connect("http://host/capture").capture(...)` and `@shipping.connect("http://host/book").book(...)` POST `{"kind", "payload"}` JSON to external HTTP endpoints and return the JSON response as an ORV value. Prod deploy persistence records static HTTP adapter URLs as `commerce_endpoints`, lists them in the generated runbook, and does not create Compose volumes for them. Env-configured HTTP handles such as `@payment.connect(@env.PAYMENT_ADAPTER_URL ?? "http://host/capture")` now record the fallback URL in `commerce_endpoints`, the env/default pair in `commerce_env`, and generated Compose/runbook operator configuration. Provider-specific schemes such as `stripe://` or `carrier://` remain explicit unsupported errors until dedicated provider integrations exist.

**Shop scaffold delta (2026-05-03):** `orv init --template shop` now writes the shopping route scaffold with a browser `GET /` HTML home route, product/member/order/payment/shipment POST forms, form-urlencoded `@body` parsing, env-overridable SQLite-backed `let shopdb = @db.connect(@env.SHOP_DATABASE_URL ?? "sqlite://data/shop.sqlite")` DB adapter persistence, and a README with check/build/verify/run-build commands, browser home URL, SQLite DB backup guidance, generated deploy runbook/Compose launch guidance, Compose `data/` persistence, and the member/payment/shipment route inventory. 2026-05-05 adds local file-backed commerce adapters, generated prod artifacts record `data/shop.sqlite` as `db_paths` and commerce paths as `record_paths`, and the deploy runbook/Compose volume keep the record logs under persistent `data/` storage. 2026-05-05 also adds a browser `GET /admin` operations dashboard with catalog/order/shipment read-model links plus visible SQLite/payment/shipping storage paths, and the generated shop README now documents `http://localhost:8080/admin`. 2026-05-06 adds reference `http://` payment/shipping adapter calls for externally hosted provider endpoints, static prod artifacts expose those URLs as `commerce_endpoints`, env-configured adapter URLs expose `commerce_env` plus Compose defaults for operator overrides, and the shop DB URL exposes `db_env` with a Compose default. The shop scaffold now calls `@payment.connect(@env.PAYMENT_ADAPTER_URL ?? "file://data/payments.jsonl")` and `@shipping.connect(@env.SHIPPING_ADAPTER_URL ?? "file://data/shipments.jsonl")`, so local file logs remain the default while deploy operators can redirect to HTTP adapters without editing source. Provider-specific schemes still fail explicitly until dedicated integrations exist. Remaining shop north-star work is native server deployment, dedicated Stripe/carrier adapters, PostgreSQL/MySQL DB adapters, and richer dynamic storefront/admin workflows.

**Deploy route inventory delta (2026-05-03):** `orv build --prod` now writes `deploy/routes.json` for server builds and `orv verify-build` checks that it matches the server runtime artifact, giving deploy/reveal tooling a standalone route inventory before native bundling.

**Deploy container contract delta (2026-05-03):** `orv build --prod` now writes `deploy/container.json`, `deploy/Dockerfile`, `deploy/compose.yaml`, and `deploy/README.md` for server builds, records reference `runtime_image` plus listen/port exposure in the container contract, emits Dockerfile `EXPOSE` and Compose port/environment wiring for static or env-default nonzero listen ports, records static `@db.wal` persistence paths and generated Compose volume mounts, includes request trace capture, live trace EventSource, and editor trace commands in the deploy runbook, and `orv verify-build` checks that the container contract points at the same runtime artifact, route inventory, entrypoint, runtime image, compose file, runbook, listen descriptor, ports, persistence contract, reference server command, and trace runbook guidance. Remaining production deploy work is native server binaries and real runtime images.

**Deploy env example delta (2026-05-06):** `orv build --prod` now writes `deploy/env.example`, links it from `deploy/manifest.json` as `server.env_example`, and requires the generated runbook to reference it. The artifact records the effective `PORT` assignment plus env/default pairs from DB and commerce adapter config such as `SHOP_DATABASE_URL`, `PAYMENT_ADAPTER_URL`, and `SHIPPING_ADAPTER_URL`; `orv verify-build` checks that the env example remains aligned with the server artifact persistence contract.

**Deploy commerce adapter contract delta (2026-05-06):** `orv build --prod` now writes `deploy/commerce-adapters.json`, links it from `deploy/manifest.json` as `server.commerce_adapters`, and requires the generated runbook to reference it. The artifact records each payment/shipping adapter kind, mode, env/default, endpoint or record path, and reference HTTP POST `{"kind","payload"}` request contract; `orv verify-build` rejects adapter contract drift from the rehydrated source bundle analysis, and reveal/editor/LSP production payloads expose the same adapter contract.

**Pipeline dependency graph delta (2026-05-05):** `NameId` now lives in the dependency-free `orv-ids` crate and is re-exported by `orv-resolve`/`orv-hir`. This keeps `orv-compiler`'s normal dependency graph at `orv-diagnostics`, `orv-hir`, `orv-ids`, and `serde`, without pulling resolver/parser pipeline crates into artifact generation paths.

**Native launcher build-dir delta (2026-05-05):** Generated `server/native/main.rs` still accepts `ORV_BUILD_DIR` as an explicit override, but can now infer the build directory when run from `server/native/target/release/orv-native-server`. Generated deploy runbooks and shop README note the inference while preserving the explicit command.

**Native runtime image plan delta (2026-05-05):** Server builds now write `server/runtime-image.json`, add it to build manifest and bundle plan, link it from `server/native-server.json`, and expose it from prod `deploy/manifest.json` as `server.native_runtime_image_plan`. The artifact records the reference runtime image, planned OCI image tag, native binary path, route/listen/runtime feature shape, and `native-codegen`/`native-runtime-image` blockers; `orv verify-build` validates it against the server runtime artifact, and `orv reveal` includes the image plan summary in route-origin native production payloads.

**Native route source delta (2026-05-05):** Server builds now write `server/native/routes.rs`, add it to build manifest and bundle plan as `native_server_routes_source`, link it from `server/native-server.json` as `routes_source`, and expose it from prod `deploy/manifest.json` as `server.native_routes_source`. The generated Rust source records route method/path/origin id constants plus `:param`/`:rest*`-aware `orv_native_match_route`, route param capture structs, and `orv_native_param_value` lookup helper for future native router codegen; `orv verify-build` rejects route source drift, and `orv reveal` includes the route source summary in native production payloads.

**Native router source delta (2026-05-05):** Server builds now also write `server/native/router.rs`, add it to build manifest and bundle plan as `native_server_router_source`, link it from `server/native-server.json` as `router_source`, and expose it from prod `deploy/manifest.json` as `server.native_router_source`. The generated Rust source consumes `routes.rs`, delegates matched route dispatch to native handlers while carrying route origin id, first response origin id, and params, and returns 404 for unmatched routes. `orv verify-build` rejects router source drift, and `orv reveal` includes the router source summary plus a `response_origin_dispatch` contract check in native production payloads.

**Native handler source delta (2026-05-05):** Server builds now also write `server/native/handlers.rs`, add it to build manifest and bundle plan as `native_server_handlers_source`, link it from `server/native-server.json` as `handlers_source`, and expose it from prod `deploy/manifest.json` as `server.native_handlers_source`. The generated source records a route/response origin handler descriptor table, centralizes the body-lowering pending 501 placeholder in `orv_native_handle_route`, and lets `server/native/router.rs` delegate matched route dispatch to handlers while preserving route origin, first response origin, and params. `orv verify-build` rejects handler source drift, and `orv reveal` includes handler source summary with response-origin dispatch and body-lowering placeholder flags.

**Native static response lowering delta (2026-05-05):** `server/app.orv-runtime.json` route descriptors now include `responses` metadata for contained `@respond` nodes, with static literal status and JSON body when the response expression is literal-only. `server/native/handlers.rs` uses that metadata to emit direct static JSON responses for simple route bodies and keeps the 501 body-lowering placeholder only for dynamic or unsupported responses.

**Native direct launcher delta (2026-05-06):** Generated `server/native/main.rs` now validates the native plan/server artifact and chooses execution by handler coverage. All-static route artifacts serve a dependency-free `std::net::TcpListener` HTTP/1 loop directly through generated `routes.rs`/`router.rs`/`handlers.rs`, returning generated bodies with route/response origin headers. Dynamic or unsupported route bodies keep the reference `orv run-artifact` bridge so shop/fullservice behavior remains deployable until dynamic native lowering exists.

**Native route-param response delta (2026-05-06):** `@respond` object bodies whose fields are direct route-param reads such as `{ id: @param.id }` now lower into `server/app.orv-runtime.json` as `route_param_json` metadata and into `server/native/handlers.rs` as direct JSON string assembly using captured route params. Numeric casts such as `{ id: @param.id as int }` and `{ weight: @param.weight as float }` lower to JSON numbers with generated parse guards, and integer add/sub/mul/div/rem literal or captured-operand responses such as `{ next: (@param.id as int) + 1 }`, `{ prev: (@param.id as int) - 1 }`, `{ doubled: (@param.id as int) * 2 }`, `{ half: (@param.id as int) / 2 }`, `{ parity: (@param.id as int) % 2 }`, or `{ shifted: (@param.id as int) + (@param.offset as int) }` lower to checked native numeric JSON output. Artifacts whose every route is static JSON, route-param JSON, query-param JSON, route/query numeric-cast JSON, route/query int literal/captured arithmetic JSON, request-body JSON passthrough, or request-body field JSON now use the direct native HTTP launcher; DB, unsupported arithmetic beyond these lowered slices, and unsupported casts still keep the reference fallback.

**Native query-param response delta (2026-05-06):** Direct query-param response objects such as `{ q: @query.q }` now lower into `query_param_json` metadata and generated native handlers. Numeric casts such as `{ page: @query.page as int }` and `{ ratio: @query.ratio as float }` lower to JSON numbers with generated parse guards, and integer add/sub/mul/div/rem literal or captured-operand responses such as `{ next: (@query.page as int) + 1 }`, `{ prev: (@query.page as int) - 1 }`, `{ doubled: (@query.page as int) * 2 }`, `{ half: (@query.page as int) / 2 }`, `{ parity: (@query.page as int) % 2 }`, or `{ next: (@query.page as int) + (@query.step as int) }` lower to checked native numeric JSON output. The direct launcher parses and form-decodes URL query strings before dispatching through the native router, so artifacts made only of static, route-param, query-param, route/query numeric-cast, query-param int literal/captured arithmetic, request-body passthrough, and request-body field JSON handlers no longer need the reference bridge. DB, unsupported arithmetic beyond this query lowered slice, and unsupported casts still keep the fallback.

**Native request-body response delta (2026-05-06):** Direct request-body passthrough objects such as `{ received: @body }` now lower into `request_body_json` metadata and generated native handlers. The direct launcher reads HTTP `Content-Length`, carries the raw JSON body through `orv_native_dispatch_with_request`, and embeds that body as a JSON value in the response; DB-backed handlers and other dynamic bodies still keep the reference fallback.

**Native request-body-field response delta (2026-05-06):** Direct request-body field objects such as `{ handle: @body.handle }` now lower into `request_body_field_json` metadata and generated native handlers. The direct launcher decodes shallow JSON object fields and `application/x-www-form-urlencoded` body pairs into `body_fields`, and handlers emit those fields through the same JSON string escaping path as route/query params. 2026-05-06 also lowers integer and float casts such as `{ quantity: @body.quantity as int }` and `{ amount: @body.amount as float }` into native numeric JSON output, request-body integer add/sub/mul/div/rem responses such as `{ quantity: (@body.quantity as int) + 1 }`, `{ due: (@body.total as int) - (@body.discount as int) }`, `{ cents: (@body.quantity as int) * 100 }`, `{ share: (@body.total as int) / (@body.parts as int) }`, and `{ remainder: (@body.total as int) % (@body.parts as int) }`, body-field integer multiplication such as `{ total: (@body.quantity as int) * (@body.unit_price as int) }`, and body-field float arithmetic such as `{ total: (@body.price as float) * (@body.quantity as float) }` into checked native numeric JSON output. DB-backed handlers, unsupported arithmetic expressions, unsupported casts, and nested dynamic body expressions still keep the reference fallback.

**Native empty response delta (2026-05-06):** Static `@respond` payloads with 1xx/204/304 statuses and explicit `void` payloads now lower into `empty` metadata and generated native handlers. The direct HTTP response writer suppresses body bytes and body `content-type` for those statuses to match reference runtime HTTP semantics.

**Native mixed object response delta (2026-05-06):** Single-response `@respond` object bodies that mix static literal fields with direct request-domain fields, such as `{ err: "product_not_found", sku: @body.sku }`, now lower into ordered `mixed_json` metadata and generated native handlers. Mixed objects also preserve supported captured numeric operations, so shapes such as `{ kind: "calc", next_id: (@param.id as int) + 1, prev_page: (@query.page as int) - 1 }` stay on the direct native path instead of falling back to the reference bridge.

**Native guarded response flow delta (2026-05-06):** Simple route handlers shaped as one or more `if @body.field == "literal" { @respond ... }`, `if @body.token == @query.token { @respond ... }`, `if (@body.quantity as int) > 0 { @respond ... }`, `if (@body.quantity as int) <= (@body.stock as int) { @respond ... }`, `if (@body.amount as float) > 0.0 { @respond ... }`, `if (@body.amount as float) <= (@query.limit as float) { @respond ... }`, `if @param.kind == "sale" { @respond ... }`, `if @query.mode != "compact" { @respond ... }`, or `if @body.left != @body.right { @respond ... }` guards followed by an unconditional `@respond` now lower response guard metadata into `server/app.orv-runtime.json` and generated native handlers. The direct launcher serves these guarded multi-response routes without the reference bridge; dynamic guarded routes with DB calls, unsupported casts, or unsupported branch shapes still fall back.

**Native response origin delta (2026-05-05):** Server route descriptors now include contained `@respond` origin ids as `response_origin_ids`, and generated `server/native/routes.rs` mirrors them in `OrvNativeRoute`. This gives future native handler codegen and production trace/reveal tooling a stable route-to-response origin contract before body lowering exists.

**Client WASM source binding delta (2026-05-03):** Interactive client builds now embed the source bundle entry and stable source bundle hash in both `ORV_CLIENT_BOOTSTRAP` and the `orv.client` WASM custom section. The generated JS bootstrap fetches `source-bundle.json`, verifies its stable hash before instantiating WASM, and `orv verify-build` rejects client JS/WASM metadata that no longer matches the source bundle contract. Remaining client work is dynamic/reactive WASM/JS codegen beyond the current source-bound initial-render module.

**Client bundle manifest delta (2026-05-05/2026-05-06):** Interactive client builds now write `client/manifest.json` and `client/reactive-plan.json`, add both to build manifest and bundle plan, link the manifest from prod `deploy/manifest.json` as `client.manifest`, and expose both from `orv reveal` client production payloads. The manifest binds page shell, reactive plan, JS loader, WASM module path/hash, source bundle hash, WASM exports, initial-render metadata, runtime features, and `dynamic-client-codegen`/`reactive-dom-diff` blockers into one checked client bundle contract. The reactive plan records source-backed `let sig` origins, top-level signal initial state metadata, checked initial-render binding from `client/app.wasm` to `pages/index.html`, per-signal `signal_state` bindings to `client/app.js`, source-backed `signal_text` bindings to initial DOM text slots and single-signal interpolated text templates, source-backed `signal_attr` bindings to initial DOM attributes/properties, single-signal interpolated attribute templates, and single-signal conditional attributes, source-backed `signal_event` bindings for simple event-time signal assignments, boolean toggles, form input target-value updates, and numeric target-value casts, source hash, and future full DOM-diff blocker. Generated `client/app.js` now fetches the manifest and reactive plan before source bundle/WASM loading, rejects schema/source hash/WASM path/hash/export/reactive-plan/signal-state/signal-text/signal-attr/signal-event binding mismatches, creates the initial client reactive state, binds matching text slots/templates, attributes/templates/conditions, and event handlers, exposes `__ORV_SET_SIGNAL__`, validates the decoded WASM initial render hash and byte length against the manifest, and records reactive signal count, signal-state binding count, DOM binding count, attr binding count, event binding count, and state hash on the client root. `orv verify-build` validates the manifest/reactive plan against the generated page/loader/WASM/source bundle and verifies that the loader consumes manifest, reactive-plan, source-bundle, WASM hash, initial-render, and reactive-state contracts; reveal production payloads include the client manifest `wasm_hash`. Remaining client work is full dynamic/reactive WASM/JS codegen beyond the current source-bound initial-render and signal update module.

**Server listen artifact delta (2026-05-03):** origin maps now include `listen` nodes, server runtime artifacts preserve listen origin/static/env port descriptors, `server/launch.json` must match that listen descriptor during `orv verify-build`, and prod builds reject static test-only `@listen 0`. Remaining deployment work is native runtime images.

**Graph view delta (2026-05-03):** `orv graph <file> --view --out <dir>` now writes `graph.json` plus a static `index.html` graph view with source/semantic depth stats, SVG node/edge visualization, ProjectGraph node rows, and semantic origin rows. 2026-05-05 adds node search and kind filtering to the ProjectGraph view, plus member/edge search filtering to the workspace graph view. `orv workspace graph --view --out <dir>` writes `workspace-graph.json` plus a static `index.html` workspace member/path-dependency graph view. Remaining graph work is richer layout.

**Package lock/workspace delta (2026-05-03):** `orv lock [dir-or-orv.toml]` now reads `[project]`, `[dependencies]`, and `[dev-dependencies]` metadata from `orv.toml`, writes deterministic JSON `orv.lock` entries with sorted package names, registry/path source metadata, optional `auth_token_env` env var names without secret tokens, prerelease/build-aware exact versions, local/file/HTTP/HTTPS registry `index.json` resolution for wildcard/caret/tilde/comparator/disjunction ranges with `requested_version` preservation, and stable `fnv1a64` checksums, and `--check` verifies the committed lockfile without rewriting it. `orv fetch` now verifies that lockfile against the manifest and materializes path/local-registry dependencies plus optional Bearer-auth HTTP/HTTPS registry `source-bundle.json` endpoints into source-bundle cache artifacts and `deps-manifest.json`. `orv add/remove` now edits the selected dependency section and regenerates the lockfile. `orv workspace new` creates a basic member project and records it in root `[workspace].members` with resolver `2`. `orv workspace graph` emits member ProjectGraph/files/dependencies plus path dependency edges as a workspace-wide input artifact, records target member name/version and requested version on workspace path dependency edges, and rejects target version mismatches. `orv workspace lock` now orders members dependency-first and writes `members/<member>/orv.lock` plus `workspace-lock.json` artifacts without mutating member source directories. `orv workspace fetch` materializes those member lockfiles into per-member dependency caches plus `workspace-fetch.json`. `orv workspace build` now reuses the existing member build pipeline, orders members dependency-first from path dependency edges, verifies each member build directory, writes `workspace-build.json` plus `workspace-graph.json`, and supports `--incremental` skip for unchanged source-bundle input hashes while rebuilding dependents when dependencies rebuild. Remaining package work is richer cross-member dependency semantics beyond direct path version validation.

**DAP exception filter delta (2026-05-03):** `orv dap serve --stdio` now stores `setExceptionBreakpoints` diagnostics/runtime selections and only marks launch stops as `exception` when the active filter covers the runtime status. Remaining DAP work is richer editor UI wiring.

**DAP cancel/instruction-breakpoint delta (2026-05-03):** `orv dap serve --stdio` now advertises and accepts DAP `cancel` requests as synchronous no-op success responses, avoiding client-visible unsupported-command errors for adapters that send cancellation frames. It also accepts `setInstructionBreakpoints` and returns explicit unverified breakpoint entries because ORV currently exposes source runtime frames rather than stable instruction addresses.

**DAP disassemble delta (2026-05-03):** `orv dap serve --stdio` now advertises `supportsDisassembleRequest` and maps `disassemble` requests for `orv:frame:<n>` memory references to source-frame pseudo instructions with source location, line, and stable `orv:frame:<n>` addresses. ORV still does not expose native instruction addresses.

**DAP logpoint delta (2026-05-03):** `setBreakpoints` now treats `logMessage` source breakpoints as non-stopping logpoints, emitting console `output` events when the captured runtime frame reaches the line while preserving condition and hit-condition filtering.

**DAP attach request delta (2026-05-03):** `orv dap serve --stdio` now accepts the standard DAP `attach` request and routes it through the existing launch pipeline with `attachRuntime=true`, so attach configurations can use child-process or `attachRuntimeMode="inProcess"` server transports without relying on custom launch arguments.

**DAP read memory delta (2026-05-03):** `orv dap serve --stdio` now advertises `supportsReadMemoryRequest` and maps `readMemory` over `orv:frame:<n>` source-frame memory references, returning base64 source-line byte slices from the launch-time project snapshot.

**Editor DAP native-host breakpoint delta (2026-05-05):** `orv editor export` now mirrors executable breakpoint runner commands into `native-host.json` under `debug.breakpoint_commands`, alongside source path, line, and `setBreakpoints` request preview. Native editor hosts can now wire line-level breakpoint actions from the manifest without reparsing `state.json` or the static shell.

**Editor DAP native-host configuration delta (2026-05-06):** `orv editor export` now mirrors launch/live/attach debug configurations into `native-host.json` under `debug.configurations` with `configuration_count`, matching the existing control and breakpoint command inventories so native editor hosts can wire DAP launch surfaces without reparsing `state.json`.

**LSP signature help delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/signatureHelp`, returning function parameter labels, return type, and active parameter index for in-call cursor positions.

**LSP inlay hint delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/inlayHint`, emitting parameter-name hints for direct function calls.

**LSP navigation delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/declaration` using the existing project-graph declaration lookup and `textDocument/typeDefinition` for struct/enum/type alias targets.

**LSP call hierarchy delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/prepareCallHierarchy`, `callHierarchy/outgoingCalls`, and `callHierarchy/incomingCalls` for direct function calls discovered from the launch project source snapshot.

**LSP implementation delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/implementation`, returning the concrete symbol location for ORV's interface-free declaration model.

**LSP type hierarchy delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/prepareTypeHierarchy`, `typeHierarchy/supertypes`, and `typeHierarchy/subtypes` for struct/enum/type alias symbols. ORV has no inheritance model yet, so supertype/subtype queries return empty arrays.

**LSP moniker delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/moniker`, returning project-unique `orv` monikers for project-graph struct/enum/type alias/function symbols.

**LSP color provider delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/documentColor` and `textDocument/colorPresentation`, exposing hex color literals and hex replacement edits to external editors.

**LSP linked editing delta (2026-05-03):** `orv lsp serve --stdio` now advertises and handles `textDocument/linkedEditingRange`, returning same-file identifier occurrence ranges with the ORV identifier word pattern.

**Editor snapshot/reveal/runtime/export/trace delta (2026-05-03):** `orv editor snapshot <file>` now emits first-party editor bootstrap JSON with diagnostics, shared ProjectGraph, graph-backed Files/Routes/Schema/Domains panel inputs, and source-hash live refresh watch sources. `orv editor reveal <dir> <origin-id>` now converts build artifact origins into first-party editor focus/source/production navigation payloads. `orv editor runtime <file>` now reuses DAP trace/runtime helpers to emit runtime status/stdout/frame inspection pane JSON. `orv editor export <file> --out <dir>` now writes `state.json`, `debug/session-runner.json`, `native-host.json`, plus a static `index.html` editor shell artifact with rendered ProjectGraph view, panel lists, selectable runtime frame detail, DAP launch/live/attach config detail, executable breakpoint sources, selectable trace detail, and optional trace navigation state with `--build <dir> --trace <trace.json>`. `native-host.json` points native editor hosts at the shell/state/debug runner artifacts, DAP adapter command, runner command, and trace/debug capabilities. `orv editor trace <dir> --trace <trace.json>` now expands captured request trace frames into editor source/production navigation payloads and embeds stable server EventSource trace transport metadata when available. `orv editor trace-stream <dir> --events <trace-events.sse>` now consumes native-host EventSource bodies and normalizes `orv:trace` snapshots plus `orv:trace.frame` deltas into editor trace stream JSON. Remaining editor work is interactive/native UI rendering and richer native production trace UI.

**DAP request trace JSON delta (2026-05-03):** In-process attached runtime request frames now expose `runtimeRequestTrace` through DAP variables/evaluate/completions as `orv.production.trace` JSON, so captured traffic can feed `orv editor trace` without scraping display strings. `launch.arguments.runtimeRequestTracePath` now flushes the same trace JSON file on pause/terminate/disconnect and exposes the path through variables/evaluate/completions. The trace JSON schema/file writer is now owned by `orv-runtime` and reused by DAP file/display surfaces instead of being duplicated inside the CLI.

**Runtime request trace file delta (2026-05-03):** Normal `@server` runtime execution now honors `orv run-artifact/run-build --trace <path>` and `ORV_RUNTIME_REQUEST_TRACE_PATH`, writing the same `orv.production.trace` file on graceful shutdown so run/build/deploy processes have a non-DAP production trace capture path. The reference runtime now serves `/__orv/trace/events` as an open-ended EventSource trace stream: it sends the current snapshot first, keeps the connection open, and emits per-frame `orv:trace.frame` events for later requests. `orv editor trace-stream` consumes those snapshot/frame bodies from native hosts. Remaining trace work is richer native trace UI.

**Editor trace summary delta (2026-05-03):** `orv editor trace` now adds per-frame request labels, route/status classes, aggregate status buckets, and a trace-file-hash live refresh contract so the exported editor shell can render captured traffic without recomputing request summaries in UI code. The exported shell also renders status buckets and client-side trace filters for all/2xx/3xx/4xx/5xx/other traffic.

**Editor runtime frame inspector delta (2026-05-03):** `orv editor export` now renders runtime frame counts, selectable runtime frame rows, and a selected-frame detail pane sourced from the same DAP runtime frame JSON used by `orv editor runtime`, including source line, locals, call stack, and per-frame output.

**Editor DAP wiring delta (2026-05-03):** `orv editor export` now embeds `orv dap serve --stdio` adapter command metadata, launch/live-launch/attach configuration JSON, continue/pause/step/restart/disconnect request payloads, native-host `session_runner` command/control metadata, executable source breakpoint lines, selectable debug config/control rows, and `setBreakpoints` request previews in the static shell. `orv editor debug <file> --control <...>` now drives initialize/live launch/control/stackTrace through the same Content-Length DAP transport and returns response/event frames for native editor control wiring; repeated controls reuse one session. `orv editor run-debug <state-or-runner.json> --control <...>` now executes the exported `debug.session_runner` or standalone `debug/session-runner.json` artifact against the recorded source program and returns runner result JSON for native host integration. 2026-05-05 adds exact per-control runner command wiring: `state.debug.controls[*].runner_command`, `debug/session-runner.json` control commands, `native-host.json` `debug.control_commands`, and a static-shell Runner Command pane all point at the same `orv editor run-debug ... --control <name>` path. 2026-05-06 adds a stable `debug/session-result.json` result artifact path to the exported runner/native-host contract, and `orv editor run-debug debug/session-runner.json` now writes the latest runner result there for native host UI refresh. Remaining DAP/editor work is richer interactive UI rendering and richer trace UI.

**Editor DAP breakpoint execution delta (2026-05-05):** `orv editor debug` and `orv editor run-debug` now accept repeated `--breakpoint <path>:<line>` arguments before controls, send DAP `setBreakpoints` requests in the same live session, and return breakpoint response summaries next to control and stack results. `orv editor export` adds per-breakpoint runner commands and request previews, and `native-host.json` exposes the breakpoint argument format and count for native editor UI execution.

**Editor trace native transport delta (2026-05-05):** Editor export with trace state now mirrors stable server EventSource trace transport into `native-host.json` as `trace.transport` and renders the same endpoint in a static-shell Trace Transport pane, giving native hosts a checked contract for `/__orv/trace/events` consumption.

**Editor trace stream runner delta (2026-05-05):** Trace-enabled editor exports now add `trace.stream_runner` to both `state.json` and `native-host.json`, with the checked command `orv editor trace-stream <build-dir> --events trace/events.sse`; the static shell renders a Trace Stream Runner pane so native hosts can pipe captured EventSource bodies back into the same editor trace-stream normalizer.

**Editor trace native frame inventory delta (2026-05-06):** Trace-enabled editor exports now mirror `trace.status_filters` and `trace.frames[*]` into `native-host.json`, including request summary, origin id, and source/production navigation payloads, so native editor hosts can render trace lists without reparsing the full `state.json`. Trace frames now also flatten `source`, `production`, and `reveal_command` for source-backed frames, giving native hosts a direct `orv editor reveal <build-dir> <origin-id>` action for captured production requests.

---

### Task 1: Parser Surface Gap Closure

**Files:**
- Modify: `crates/orv-syntax/src/parser.rs`
- Modify: `crates/orv-syntax/src/lexer.rs`
- Test: `crates/orv-syntax/src/parser.rs`

- [x] Write failing parser tests for domain `key=value`, reserved prop names, shorthand lambdas, compound assignment, index assignment, optional chaining, inline object array types, and string/union type aliases.
- [x] Run focused parser tests and verify they fail on current parser behavior.
- [x] Implement the smallest AST-compatible parsing changes, preferring existing `ExprKind`/`TypeRefKind` where possible and preserving unsupported details as named/opaque forms.
- [x] Run focused parser tests and fixture checks for `fixtures/default-syntax.orv`, `fixtures/plan/03-domains.orv`, `fixtures/plan/04-web.orv`, `fixtures/plan/05-server.orv`.

### Task 2: Analyzer/Runtime Core Domain Gap Closure

**Files:**
- Modify: `crates/orv-analyzer/src/lib.rs`
- Modify: `crates/orv-runtime/src/interp.rs`
- Modify: `crates/orv-runtime/src/server.rs`
- Test: `crates/orv-runtime/src/server.rs`

- [x] Write failing checks for `@serve ./path`, `@db.find User { @where ... }`, `%data=...`, and HTML prop/event preservation.
- [x] Implement conservative lowering/runtime adapters for core server/web/db syntax used by `plan/04` and `plan/05`.
- [x] Add stable reference behavior for advanced domains where useful (`@offline`, `@cache`, `@net`, `@plugin`, `@gpu`, `@observability`, `@ffi`) while keeping non-core guarantees documented as roadmap.
- [x] Verify all current e2e fixtures still pass.

### Task 3: Fixture Gate

**Files:**
- Modify only if needed: `fixtures/plan/*.orv`, `fixtures/default-syntax.orv`
- Test: `orv-cli check`

- [x] Run `orv-cli check` across `fixtures/default-syntax.orv`, `fixtures/plan/01-basics.orv` through `fixtures/plan/09-shopping-mall.orv`, and `fixtures/e2e/*.orv`.
- [x] Classify any remaining failures as implementation gaps or intentionally future-only examples.
- [x] Add narrow syntax/runtime tests for each remaining implementation gap before fixing it.
- [x] Finish with `rtk timeout 120 cargo test`, `rtk cargo clippy --all-targets`, `rtk cargo fmt --check`, and `rtk git diff --check`.
